start hi_there;

func hi_there() -> int {
    //bismach(1 + 3 * 40 / 8, "hi there :)");

    string s = "hello\n\tworld";
    //print("hello\n\tworld");
    s = "Eden";

    //bismach(-123, true);

    return 0;
}

func bismach(int num, bool f) -> bool {
    println(num);
    bool x = true;
    bool y = false;
    println("x = ", x, "\ty = ", y);
    swap x, y;
    println("x = ", x, "\ty = ", y);
    return (x or y) and f;
}

func test() -> void {
    loop i: 1 to 6 times {
        loop i times {
            print("*");
        }
        println();
    }
}

// one-line functions :D
func pow(int x) -> int => x * x;
