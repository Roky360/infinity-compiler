start main;

func main() -> int {
    int x = 3;
    int p = 23;
    //x = 5*2^(x! / 1 - 2) - (-p);

    bismach(1 + 3 * 40 / 8);

    return 1;
}

func bismach(int x) -> int {
    int x = 5 + 40 / 8;
    int y = x - 7;

    return y;
}

// one-line functions :D
func pow(int x) -> int => x * x;
